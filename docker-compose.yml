services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: finuser
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: financoach
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      DATABASE_URL: postgresql://finuser:devpassword@db:5432/financoach
    depends_on:
      - db

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    command: npm run dev

volumes:
  pgdata:
